<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.nkr.fashionita.ui.fragment.search.SearchResultViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorWhite"
        android:orientation="vertical"
        android:layout_marginLeft="@dimen/spaceNormal"
        android:layout_marginRight="@dimen/spaceNormal"

        >

        <!--toolbar-->
        <include
            android:id="@+id/layout_toolbar"
            layout="@layout/toolbar_search_searchresult"
            bind:viewModel = "@{viewModel}"
            />


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spaceSmall"
            android:text="@string/saved_search"
            android:textSize="@dimen/textsize_large"
            android:fontFamily="@font/poppins_bold"
            />

<!--
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rv_saved_search"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                adapterGridHome="@{viewModel.search_adapter}"
                android:overScrollMode="never"
                />
            -->


        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_saved_search"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            adapterGridHome="@{viewModel.search_adapter}"
            android:overScrollMode="never"
            />




    </LinearLayout>




</layout>

